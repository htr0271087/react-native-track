import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

export const AccountScreen = () => {
    return (
        <Text style={{ fontSize: 18 }}>Account Scree</Text>
    )
}

const styles = StyleSheet.create({});

export default AccountScreen;