import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

export const SigninScreen = () => {
    return (
        <Text style={{ fontSize: 18 }}>Signin Scree</Text>
    )
}

const styles = StyleSheet.create({});

export default SigninScreen;